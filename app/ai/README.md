# ai

AI and vector store integration used by services (e.g. pantry embeddings, search).

## Modules

- **vector_store.py** — Vector store factory/access (e.g. for semantic search); uses `app.utils.embedding` and `app.deps.supabase`.
